<?xml version="1.0"?>

<launch>
  <arg name="config"/>
  <arg name="use_sim_time" default="false"/>

  <!-- use simulation time -->
  <param name="/use_sim_time" value="$(arg use_sim_time)" type="bool"/>

  <param name="robot_proc_var" type="double" value="100"/>
  <param name="robot_meas_var" type="double" value="0.001"/>

  <!-- MPC -->
  <include file="$(find upright_ros_interface)/launch/mpc.launch" >
    <arg name="config" value="$(arg config)"/>
  </include>

  <!-- MRT -->
  <node pkg="upright_ros_interface" type="mrt_node" name="mrt_node" output="screen" required="True" args="$(arg config)" />

  <!-- Kalman filter estimator. -->
  <include file="$(find upright_ros_interface)/launch/projectile_estimator.launch"></include>
</launch>
