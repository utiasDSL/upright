<launch>
  <arg name="test"        default="true"/>
  <arg name="rviz"        default="true"/>
  <arg name="rvizconfig"  default="$(find tray_balance_ocs2)/rviz/mobile_manipulator.rviz"/>
  <arg name="urdf"       default="$(find tray_balance_ocs2)/urdf/mm.urdf"/>

  <param name="robot_description" command="$(find xacro)/xacro --inorder $(arg urdf)"/>
  <param name="use_gui" value="true"/>

  <node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher"/>
  <node if="$(arg test)" name="joint_state_publisher" pkg="joint_state_publisher" type="joint_state_publisher"/>
  <node pkg="rviz" type="rviz" name="rviz" args="-d $(arg rvizconfig)" output="screen" if="$(arg rviz)"/>
</launch>
